spring.application.name=aurora-inventory
server.port=${PORT:8080}

spring.profiles.active=h2

spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173,https://aurorainventory.vercel.app}
